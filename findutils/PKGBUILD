FINDUTILS_VER=4.8.0
source=(https://ftp.gnu.org/gnu/findutils/findutils-${FINDUTILS_VER}.tar.xz)

build() {
	cd findutils-${FINDUTILS_VER}

	./configure --prefix=/tools

	make

	make check

	make install
}
