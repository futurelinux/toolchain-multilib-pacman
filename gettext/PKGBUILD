GETTEXT_VER=0.21
source=(https://ftp.gnu.org/gnu/gettext/gettext-${GETTEXT_VER}.tar.xz)

build() {
	cd gettext-${GETTEXT_VER}

	./configure --disable-shared

	make

	cp -v gettext-tools/src/{msgfmt,msgmerge,xgettext} /tools/bin
}
